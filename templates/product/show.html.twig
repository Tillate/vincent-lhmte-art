{% extends 'base.html.twig' %}

{% block title %}{{ product.name }} - Vincent LHMTE Art{% endblock %}

{% block content %}
    <div class="">

        <div class="">
                <img src="/uploads/{{ product.illustration }}" alt="{{ product.name }}" width="300px" height="400px">
        </div>

        <div class="">
            <h3>{{ product.name }} </h3>
            <p>{{ product.subtitle }}</p>
            <span class="product-page-price">{{ (product.price / 100)|number_format(2, ',', '.') }} €</span>
            <hr>
            <p>{{ product.description }}</p>
            <a href="{{ path('add-to-cart', { 'id': product.id })}}" class="add-cart">Ajouter au panier</a>
        </div>
    </div>
    <hr>
    <h3>Nos meilleurs ventes</h3>
    <p>Decouvrez les articles les plus vendus.</p>
    <div class="">
        {% for product in products %}
            <div class="">
                {% include "product/single_product.html.twig" %}
            </div>
        {% endfor %}
    </div>
    

{% endblock %}